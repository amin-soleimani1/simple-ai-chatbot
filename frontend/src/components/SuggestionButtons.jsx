import { LampDesk, PanelTop, Projector, Wrench } from "lucide-react";

const initialSuggestions = [{ label: "قیمت لامپ‌های با گارانتی", icon: LampDesk }, { label: "قیمت لامپ‌های بدون گارانتی", icon: LampDesk }, { label: "قیمت پروژکتور", icon: Projector }, { label: "قیمت تعمیرات", icon: Wrench }, { label: "قیمت پنل سقفی", icon: PanelTop }];
const conversationSuggestions = [{ label: "پروژکتور", icon: Projector }, { label: "پنل سقفی", icon: PanelTop }, { label: "تعمیرات", icon: Wrench }];
function Suggestion({ label, icon: Icon, onSelect, compact = false }) { return <button type="button" onClick={() => onSelect(label)} className={`flex min-w-0 items-center justify-center gap-1 rounded-[14px] border border-white/10 bg-[#2a1f24] px-1.5 py-1.5 text-center text-[10px] leading-4 text-zinc-200 transition hover:border-[#d9ad4a]/40 hover:bg-[#35262d] focus:outline-none focus:ring-2 focus:ring-[#d9ad4a]/70 sm:px-2 sm:text-xs ${compact ? "h-8 whitespace-nowrap px-1 text-[1.0px] sm:h-9 sm:px-2 sm:text-[1.5px]" : ""}`}><Icon size={13} className="shrink-0 text-[#e3bd62]" /><span>{label}</span></button>; }
export default function SuggestionButtons({ onSelect, inConversation }) { const suggestions = inConversation ? conversationSuggestions : initialSuggestions; if (inConversation) return <div className="mx-auto grid w-full max-w-4xl grid-cols-3 gap-2">{suggestions.map((suggestion) => <Suggestion key={suggestion.label} {...suggestion} onSelect={onSelect} />)}</div>; return <div className="mx-auto flex w-full max-w-4xl flex-col gap-2"><div className="grid grid-cols-2 gap-2">{suggestions.slice(0, 2).map((suggestion) => <Suggestion key={suggestion.label} {...suggestion} compact onSelect={onSelect} />)}</div><div className="grid grid-cols-3 gap-2">{suggestions.slice(2).map((suggestion) => <Suggestion key={suggestion.label} {...suggestion} compact onSelect={onSelect} />)}</div></div>; }
