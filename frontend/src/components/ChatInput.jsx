import { ArrowUp, LoaderCircle } from "lucide-react";
import { useState } from "react";

export default function ChatInput({ onSend, isSending }) { const [value, setValue] = useState(""); function submit(event) { event.preventDefault(); if (value.trim() && !isSending) { onSend(value); setValue(""); } } function handleKeyDown(event) { if (event.key === "Enter" && !event.shiftKey) submit(event); } return <footer className="shrink-0 px-3 pb-2 pt-1 sm:px-5 sm:pb-3 lg:px-8"><form onSubmit={submit} className="mx-auto flex w-full max-w-4xl items-end gap-2 rounded-[20px] border border-white/10 bg-[#2d2026] p-1.5 shadow-2xl shadow-black/30 focus-within:border-white/20 sm:p-2"><textarea value={value} onChange={(event) => setValue(event.target.value)} onKeyDown={handleKeyDown} rows={1} placeholder="سؤالت رو بپرس ..." aria-label="پیام شما" className="max-h-40 min-h-9 flex-1 resize-none bg-transparent px-3 py-1.5 text-sm text-zinc-100 outline-none placeholder:text-zinc-500 sm:min-h-10 sm:py-2" /><button type="submit" disabled={!value.trim() || isSending} aria-label="ارسال پیام" className="grid size-9 shrink-0 place-items-center rounded-[15px] bg-[#d9ad4a] text-[#21180d] transition hover:bg-[#e3bd62] disabled:cursor-not-allowed disabled:bg-zinc-600 disabled:text-zinc-400 sm:size-10">{isSending ? <LoaderCircle size={18} className="animate-spin" /> : <ArrowUp size={18} />}</button></form><p className="mt-1.5 text-center text-[11px] leading-5 text-zinc-500">هر روز در ساعت 18 الی 22 | بوشهر، عالی‌شهر، فاز 2، خیابان امداد</p></footer>; }
